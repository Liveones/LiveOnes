"use client";
import {useEffect,useState} from "react";
import {getTrades,type Trade} from "@/lib/store";
export default function Performance(){const[t,setT]=useState<Trade[]>([]);useEffect(()=>setT(getTrades()),[]);const w=t.filter(x=>x.result==="win").length,l=t.length-w,rate=t.length?Math.round(w/t.length*100):0;return <section><p className="eyebrow">PERFORMANCE REPORT</p><h1>Performance</h1><div className="grid4"><div className="card"><div className="cardlabel">Win rate</div><div className="value mint">{rate}%</div></div><div className="card"><div className="cardlabel">Wins</div><div className="value">{w}</div></div><div className="card"><div className="cardlabel">Losses</div><div className="value">{l}</div></div><div className="card"><div className="cardlabel">Trades</div><div className="value">{t.length}</div></div></div><div className="panel"><h2>What is working?</h2><p className="muted">As your journal grows, this section can compare times, setups, confirmations and strategy adherence.</p><div className="kpis"><span className="pill">Volume</span><span className="pill">Structure</span><span className="pill">Trend</span><span className="pill">Confirmation</span></div></div></section>}